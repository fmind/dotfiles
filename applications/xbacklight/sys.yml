---

- hosts: localhost
  become: true

  tasks:

  - name: Installing xbacklight (ubuntu)
    apt: pkg="xbacklight" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing xbacklight (fedora)
    dnf: pkg="xbacklight" state=latest
    when: ansible_distribution == 'Fedora'
