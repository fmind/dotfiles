---

- hosts: localhost
  become: true

  tasks:

  - name: Installing unison (ubuntu)
    apt: pkg="unison-gtk" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing unison (fedora)
    dnf: pkg="unison240-gtk" state=latest
    when: ansible_distribution == 'Fedora'
