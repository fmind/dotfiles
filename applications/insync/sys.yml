---

- hosts: localhost
  become: true

  tasks:

  - name: installing insync repository
    copy: src="{{playbook_dir}}/insync.repo" dest="/etc/yum.repos.d/insync.repo" owner='root' group='root' mode='0644'

  - name: installing insync
    dnf: pkg="insync" state=latest
    ignore_errors: true
