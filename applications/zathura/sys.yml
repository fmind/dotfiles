---

- hosts: localhost
  become: true

  tasks:

  - name: Installing zathura (ubuntu)
    apt: pkg="zathura" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing zathura (fedora)
    dnf: pkg="zathura-plugins-all" state=latest
    when: ansible_distribution == 'Fedora'
