---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing KDE applications
    apt: pkg="{{item}}" state=latest
    with_items:
        - krita
        - karbon
        - ktorrent
