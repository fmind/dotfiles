---

- hosts: localhost
  become: true

  tasks:

  # - name: Installing tlp (ubuntu)
  #   apt: pkg="tlp" state=latest
  #   when: ansible_distribution == 'Ubuntu'

  - name: Installing tlp (fedora)
    dnf: pkg="tlp" state=latest
    when: ansible_distribution == 'Fedora'
