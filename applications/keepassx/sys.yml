---

- hosts: localhost
  become: true

  tasks:

  - name: Installing keepassx (ubuntu)
    apt: pkg="keepassx" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing keepassx (fedora)
    dnf: pkg="keepassx" state=latest
    when: ansible_distribution == 'Fedora'
