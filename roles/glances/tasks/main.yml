---

- name: dependencies
  pip:
    name:
      - bottle
    executable: pip3
    extra_args: --user
  tags: user

- name: package
  pip:
    name: glances
    executable: pip3
    extra_args: --user
  tags: user
