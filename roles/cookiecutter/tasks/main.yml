---

- name: package
  community.general.pipx:
    name: cookiecutter
  tags: user

- name: develop
  community.general.pipx:
    name: cruft
  tags: user

- name: config
  ansible.builtin.file:
    src: "{{ role_path }}/files/cookiecutterrc"
    dest: ~/.cookiecutterrc
    state: link
    force: no
  ignore_errors: true
  tags: user
