---

- name: package
  ansible.builtin.package:
    name: silversearcher-ag
  become: true
  tags: admin

- name: ignore
  ansible.builtin.file:
    src: "{{ role_path }}/files/agignore"
    dest: ~/.agignore
    state: link
    force: yes
  tags: user
