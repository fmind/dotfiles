---

- name: package
  package:
    name: vim
  become: true
  tags: admin

- name: config
  file:
    src: "{{ role_path }}/files/vimrc"
    dest: ~/.vimrc
    state: link
    force: yes
  tags: user
