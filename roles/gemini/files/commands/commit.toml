description = "Ask the model to commit files with a Conventional Commit message."

prompt = """
Write a commit message using "Conventional Commits" for the staged files and use the shell command `git commit -m` to commit them.
"""
