---

- name: package
  pip:
    name: xonsh
    executable: pipx
  tags: user

- name: config
  file:
    force: yes
    state: link
    dest: ~/.xonshrc
    src: "{{ role_path }}/files/xonshrc"
  tags: user
