---

- name: package
  community.general.pipx:
    name: xonsh[full]
  tags: user

- name: config
  ansible.builtin.file:
    src: "{{ role_path }}/files/xonshrc"
    dest: ~/.xonshrc
    state: link
    force: true
  tags: user
