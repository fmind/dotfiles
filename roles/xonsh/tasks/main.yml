---

- name: package
  pip:
    name: xonsh
    executable: pip3
    extra_args: --user
  tags: user

- name: config
  file:
    src: "{{ role_path }}/files/xonshrc"
    dest: ~/.xonshrc
    state: link
    force: yes
  tags: user
