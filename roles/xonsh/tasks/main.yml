---

- name: package
  community.general.pipx:
    name: xonsh
  tags: user

- name: config
  ansible.builtin.file:
    src: "{{ role_path }}/files/xonshrc"
    dest: ~/.xonshrc
    state: link
    force: yes
  tags: user
