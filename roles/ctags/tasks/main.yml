---

- name: package
  package:
    name: exuberant-ctags
  become: true
  tags: admin

- name: config
  file:
    src: "{{ role_path }}/files/ctags"
    dest: ~/.ctags
    state: link
    force: yes
  tags: user
