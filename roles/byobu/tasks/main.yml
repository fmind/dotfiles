---

- name: package
  ansible.builtin.package:
    name: byobu
  become: true
  tags: admin

- name: manager
  community.general.pipx:
    name: tmuxp
  tags: user

- name: directory
  ansible.builtin.file:
    path: ~/.byobu
    state: directory
    mode: 0750
  tags: user

- name: configs
  ansible.builtin.file:
    src: "{{ item }}"
    dest: "~/.byobu/{{ item|basename }}"
    state: link
    force: yes
  with_fileglob:
    - "{{ role_path }}/files/*"
  tags: user

- name: layouts
  ansible.builtin.file:
    src: "{{ role_path }}/files"
    dest: ~/.tmuxp
    state: link
    force: yes
  tags: user
