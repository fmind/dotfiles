[user]
    name = Médéric Hurier (fmind)
    email = fmind@fmind.me
    username = fmind

[core]
    editor = vim
    hooksPath = hooks
    excludesfile = ~/.gitignore

[alias]
    st = status
    addall = add --all
    tree = ls-tree -r master --name-only
