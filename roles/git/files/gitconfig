[user]
    username = fmind
    name = Médéric Hurier
    email = fmind@fmind.me

[core]
    editor = vim
	hooksPath = hooks/
    excludesfile = ~/.gitignore

[color]
    ui = auto

[merge]
    tool = vimdiff

[push]
    default = simple

[alias]
    aa = add --all
    br = branch
    ca = commit -am
    ci = commit -m
    ck = checkout
    cl = clone
    cn = commit -nm
    df = diff
    ph = push
    pl = pull
    sm = submodule
    st = status
    tr = ls-tree -r master --name-only
