[user]
    username = fmind
    name = Médéric Hurier
    email = fmind@fmind.me

[core]
    editor = vim
    hooksPath = hooks
    excludesfile = ~/.gitignore

[alias]
    st = status
    addall = add --all
    tree = ls-tree -r master --name-only
