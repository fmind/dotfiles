---

- name: package
  package:
    name: bash
  become: true
  tags: admin

- name: profile
  file:
    force: yes
    state: link
    dest: ~/.bash_profile
    src: "{{ role_path }}/files/bash_profile"
  tags: user
