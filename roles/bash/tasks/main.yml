---

- name: config
  file:
    force: yes
    state: link
    dest: ~/.bashrc
    src: "{{ role_path }}/files/bashrc"
  tags: user

- name: profile
  file:
    force: yes
    state: link
    dest: ~/.bash_profile
    src: "{{ role_path }}/files/bash_profile"
  tags: user
