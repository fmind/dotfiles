---

- name: package
  package: name='fish'
  become: true
  tags: admin
  when: false

- name: directory
  file: name='~/.config/fish/' state='directory'
  tags: user

- name: config
  file: src='{{role_path}}/files/config.fish' dest='~/.config/fish/config.fish' state=link force=yes
  tags: user
