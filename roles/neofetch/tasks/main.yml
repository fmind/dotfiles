---

- name: package
  package:
    name: neofetch
  become: true
  tags: admin

- name: directory
  file:
    name: ~/.config/neofetch
    state: directory
    mode: 0750
  tags: user

- name: config
  file:
    src: "{{ role_path }}/files/config.conf"
    dest: ~/.config/neofetch/config.conf
    state: link
    force: yes
  tags: user
