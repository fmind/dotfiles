---

- name: package
  get_url:
    url: https://raw.github.com/xwmx/nb/master/nb
    dest: ~/.local/bin/nb
    mode: '0700'
    force: yes
  tags: user

- name: config
  file:
    force: yes
    state: link
    dest: ~/.nbrc
    src: "{{ role_path }}/files/nbrc"
  tags: user
