# BINDKEY
bindkey -v
# PLUGINS
plugins=(
    ag
    alias-tips
    aws
    colored-man-pages
    command-not-found
    docker
    docker-compose
    fd
    fzf
    gh
    gitfast
    httpie
    invoke
    poetry
    pyenv
    thefuck
    vi-mode
    zoxide
    zsh-autopair
    zsh-autosuggestions
    zsh-syntax-highlighting
)
# ALIASES
if [ -f ~/.aliases ]; then
    source ~/.aliases
fi
# PRIVATE
if [ -f ~/.private ]; then
    source ~/.private
fi
# SOURCES
source $ZSH/oh-my-zsh.sh