# BINDKEY
bindkey -v
# PLUGINS
plugins=(
    alias-tips
    aws
    colored-man-pages
    command-not-found
    docker
    docker-compose
    fzf
    gcloud
    gh
    git
    gitfast
    httpie
    starship
    thefuck
    vi-mode
    zoxide
    zsh-autopair
    zsh-autosuggestions
    zsh-syntax-highlighting
)
# ALIASES
if [ -f ~/.aliases ]; then
    source ~/.aliases
fi
# PRIVATE
if [ -f ~/.private ]; then
    source ~/.private
fi
# SOURCES
source $ZSH/oh-my-zsh.sh
