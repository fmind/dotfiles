---

- name: package
  pip:
    name: mario
    executable: pipx
  when: "ansible_python_version is version('3.7', '>=')"
  tags: user

- name: dependencies
  command: pipx inject mario mario-addons
  when: "ansible_python_version is version('3.7', '>=')"
  tags: user
