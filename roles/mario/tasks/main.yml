---

- name: package
  pip:
    name: mario
    executable: pipx
  when: "ansible_python_version is version('3.7', '>=')"
  tags: user

- name: addons
  pip:
    name: mario-addons
    executable: pipx
  when: "ansible_python_version is version('3.7', '>=')"
  tags: user
