---

- hosts: localhost
  become: false

  tasks:

  - name: Deploying bash config
    file: src="{{playbook_dir}}/bashrc" dest="~/.bashrc" state=link force=yes

  - name: Deploying bash logout
    file: src="{{playbook_dir}}/bash_logout" dest="~/.bash_logout" state=link force=yes
