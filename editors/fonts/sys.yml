---

- hosts: localhost
  become: true

  tasks:

  - name: installing dejavu fonts
    package: name="dejavu-sans-mono-fonts"

  - name: installing powerline fonts
    package: name="powerline-fonts"
    when: ansible_distribution == 'Fedora'
