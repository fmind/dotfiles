---

- hosts: localhost
  become: true

  tasks:

  - name: installing dejavu fonts
    package: pkg="dejavu-sans-mono-fonts"

  - name: installing powerline fonts
    package: pkg="powerline-fonts"
    when: ansible_distribution = 'Fedora'
