---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing vim gui (ubuntu)
    apt: pkg="vim-gtk" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing vim gui (fedora)
    dnf: pkg="vim-X11" state=latest
    when: ansible_distribution == 'Fedora'
