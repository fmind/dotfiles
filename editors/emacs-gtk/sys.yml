---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing emacs gui (ubuntu)
    apt: pkg="emacs" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing emacs gui (fedora)
    dnf: pkg="emacs" state=latest
    when: ansible_distribution == 'Fedora'
