---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - shell: test -d ~/bin/idea
    ignore_errors: True
    register: ideacheck

  - name: Installing idea editor
    command: "umake ide idea ~/bin/ide"
    when: ideacheck|failed

