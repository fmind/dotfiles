---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Adding vim repository
    apt_repository: repo="ppa:jonathonf/vim" update_cache=yes
    when: ansible_distribution == 'Ubuntu'

  - name: Installing vim editor
    apt: pkg="vim" state=latest
