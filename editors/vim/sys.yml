---

- hosts: localhost
  become: true

  tasks:

  - name: installing vim (display)
    dnf: pkg="vim" state=latest
    when: lookup('env', 'DISPLAY')

  - name: installing vim (console)
    dnf: pkg="vim" state=latest
    when: not lookup('env', 'DISPLAY')
