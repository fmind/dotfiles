---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing R language (ubuntu)
    apt: pkg="r-base" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing R language (fedora)
    dnf: pkg="R-core" state=latest
    when: ansible_distribution == 'Fedora'
