---

- hosts: localhost
  become: true

  tasks:

  - name: Installing elixir
    package: name="elixir"

  - name: Installing elixir dependencies
    package: name="{{item}}"
    with_items:
      - erlang-eunit
      - erlang-erl_interface
