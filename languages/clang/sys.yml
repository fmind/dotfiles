---

- hosts: localhost
  become: true

  tasks:

  - name: Installing clang
    dnf: pkg="{{item}}" state=latest
    with_items:
      - gcc
      - make
      - cmake
      - ctags
      - clang
      - gcc-c++
