---

- hosts: localhost
  become: true

  tasks:

  - name: Installing clang
    package: pkg="{{item}}"
    with_items:
      - gcc
      - make
      - cmake
      - ctags
      - clang
      - gcc-c++
