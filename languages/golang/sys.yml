---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing go language (ubuntu)
    apt: pkg="golang" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing go language (fedora)
    dnf: pkg="go" state=latest
    when: ansible_distribution == 'Fedora'
