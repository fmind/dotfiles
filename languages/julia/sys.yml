---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing julia language (ubuntu)
    apt: pkg="julia" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing julia language (fedora)
    dnf: pkg="julia" state=latest
    when: ansible_distribution == 'Fedora'
