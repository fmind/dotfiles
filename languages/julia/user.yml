---

- hosts: localhost
  become: false

  tasks:

  - name: deploying julia configuration
    file: src="{{playbook_dir}}/juliarc.jl" dest="~/.juliarc.jl" state=link force=yes

  - name: installing julia packages
    shell: julia -e 'if Pkg.installed("{{item}}") == nothing Pkg.add("{{item}}") end'
    with_items:
      - IJulia
      - PkgDev
