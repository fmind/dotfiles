---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - name: Upgrading stack
    command: "stack upgrade"

  - name: Updating stack
    command: "stack update"

  - name: Setting-up stack
    command: "stack setup"

  - name: Installing plugins
    command: "stack install hlint hindent hoogle hasktags intero apply-refact stylish-haskell"

  - name: Configuring stack
    file: src="{{playbook_dir}}/config.yaml" dest="~/.stack/config.yaml" state=link force=yes


