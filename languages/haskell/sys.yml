---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing haskell language
    apt: pkg="haskell-stack" state=latest

  - name: Upgrading stack package manager
    command: "stack upgrade"
