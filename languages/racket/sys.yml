---

- hosts: localhost
  become: true

  tasks:

  - name: enabling racket repository
    shell: dnf copr enable -y jsouthworth/racket
    when: ansible_distribution == 'Fedora'

  - name: installing racket
    package: name="racket"
