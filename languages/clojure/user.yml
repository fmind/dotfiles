---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - name: Installing clojure language
    command: bash -lc "sdk install leiningen"

  - name: Deploying leiningen configuration file
    file: src="{{playbook_dir}}/profiles.clj" dest="~/.lein/profiles.clj" state=link force=yes

