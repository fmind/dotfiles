---

- hosts: localhost
  become: true

  tasks:

  - name: Installing nodejs (ubuntu)
    apt: pkg="nodejs" state="installed"
    when: ansible_distribution == 'Ubuntu'

  - name: Installing nodejs (fedora)
    dnf: pkg="nodejs-latest-version" state="installed"
    when: ansible_distribution == 'Fedora'
