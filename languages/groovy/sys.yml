---

- hosts: localhost
  become: true

  tasks:

  - name: Installing groovy (ubuntu)
    apt: pkg="groovy" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing groovy (fedora)
    dnf: pkg="groovy" state=latest
    when: ansible_distribution == 'Fedora'
