---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing system packages
    apt: pkg="{{item}}" state=latest
    with_items:
        - emacs
    when: ansible_distribution == 'Ubuntu'
