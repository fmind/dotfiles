description = "Refine a raw prompt for an LLM into a clearer, more reliable version."

prompt = """
Rewrite the prompt described by `{{args}}` so an LLM can follow it reliably.

Requirements:
1. Treat `{{args}}` as a rough prompt, a description of what the prompt should do, or a path to an existing prompt file.
2. Identify what is missing: role, inputs, outputs, format, constraints, edge cases, examples.
3. Rewrite the prompt with explicit sections in this order: goal, inputs, requirements, output format. Drop sections that do not apply.
4. Preserve the original intent. Do not invent requirements that were not implied.
5. Use plain, imperative language. Avoid hedging and marketing tone.
6. Output the rewritten prompt in a fenced block, then a short list of what you changed and why.
7. Do not edit files unless `{{args}}` is a path; in that case, update the file in place.
"""
