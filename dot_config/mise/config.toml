[settings]
github_attestations = false
legacy_version_file = true
slsa = false

[settings.python]
compile = false
github_attestations = false

[tools]
"aqua:abiosoft/colima" = "latest"            # Container runtimes on macOS/Linux (Go)
"aqua:docker/cli" = "latest"                 # Docker CLI utility (Go)
"aqua:docker/compose" = "latest"             # Docker Compose (Go)
"aqua:fish-shell/fish-shell" = "4.6.0"       # Fish shell (C)
"aqua:git-lfs/git-lfs" = "latest"            # Git Large File Storage (Go)
"aqua:gitleaks/gitleaks" = "latest"          # Secret scanner for Git repos (Go)
"aqua:hadolint/hadolint" = "latest"          # Dockerfile linter (Haskell)
"aqua:terrastruct/d2" = "latest"             # Declarative diagram scripting language (Go)
"aqua:turbot/steampipe" = "latest"           # Select * from cloud (Go)
"npm:@google/gemini-cli" = "0.36.0"          # Gemini CLI (TypeScript)
"npm:@google/jules" = "latest"               # AI code assistant (TypeScript)
"npm:@googleworkspace/cli" = "0.22.5"        # Workspace CLI (TypeScript)
"npm:@mermaid-js/mermaid-cli" = "11.12.0"    # Mermaid diagrams for Neovim (JavaScript)
"npm:lighthouse" = "13.1.0"                  # Web performance auditor (JavaScript)
"npm:neovim" = "5.4.0"                       # Neovim Node.js provider (JavaScript)
"pipx:ptpython" = "3.0.32"                   # Advanced Python REPL (Python)
"pipx:pynvim" = "0.6.0"                      # Neovim Python provider (Python)
ast-grep = "0.42.1"                          # Abstract-tree code search (Rust)
atuin = "18.13.6"                            # Magical shell history (Rust)
bat = "0.26.1"                               # Syntax-highlighting cat alternative (Rust)
bottom = "0.12.3"                            # Graphical process monitor (Rust)
chezmoi = "2.70.0"                           # Dotfiles manager (Go)
delta = "0.19.2"                             # Syntax-highlighting pagination (Rust)
doggo = "1.1.5"                              # Modern DNS client (Go)
duf = "0.9.1"                                # Disk usage and free space utility (Go)
dust = "1.2.4"                               # Intuitive du alternative (Rust)
eza = "0.23.4"                               # Modern ls replacement (Rust)
fastfetch = "2.61.0"                         # System info fetching tool (C)
fd = "10.4.2"                                # Fast find alternative (Rust)
fzf = "0.71.0"                               # Command-line fuzzy finder (Go)
gcloud = "563.0.0"                           # Google Cloud platform CLI (Python)
gh = "2.89.0"                                # Official GitHub CLI (Go)
jq = "1.8.1"                                 # Command-line JSON processor (C)
just = "1.49.0"                              # Modern task runner (Rust)
k9s = "0.50.18"                              # Terminal UI for Kubernetes (Go)
lazydocker = "0.25.0"                        # Terminal UI for Docker (Go)
lazygit = "0.61.0"                           # Terminal UI for Git (Go)
neovim = "0.12.1"                            # Modern Vim-based editor (C)
node = "25.9.0"                              # JavaScript runtime (C++)
pre-commit = "4.5.1"                         # Git hooks manager (Python)
python = "3.14.3"                            # Python programming language (C)
rclone = "1.73.3"                            # Cloud storage sync tool (Go)
ripgrep = "15.1.0"                           # Fast recursive grep alternative (Rust)
starship = "1.24.2"                          # Customizable cross-shell prompt (Rust)
terraform = "1.14.8"                         # Infrastructure-as-code tool (Go)
tree-sitter = "0.26.8"                       # Tree-sitter parser generator (C)
uv = "0.11.3"                                # Fast Python package installer (Rust)
xh = "0.25.3"                                # Fast HTTP request tool (Rust)
yazi = "26.1.22"                             # Fast terminal file manager (Rust)
yq = "4.52.5"                                # YAML/JSON/XML processor (Go)
zellij = "0.44.0"                            # Terminal multiplexer and workspace (Rust)
zoxide = "0.9.9"                             # Smarter cd command (Rust)
