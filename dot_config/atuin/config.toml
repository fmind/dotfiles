# Vim keys
keymap_mode = "auto"
keymap_cursor = { vim_insert = "blink-bar", vim_normal = "steady-block" }

# Search behavior
enter_accept = true

# Privacy: don't sync, keep local
auto_sync = false
update_check = false
