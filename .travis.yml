sudo: required

language: generic

services:
  - docker

# before_install:
#   - git clone https://github.com/fmind/dotfiles .dotfiles

# install:
#   - sudo apt-get install git python3-pip
#   - sudo pip3 install --upgrade pip
#   - sudo pip3 install  ansible

# matrix:
#   include:
#     - os: linux
#       dist: trusty

notifications:
  email:
    on_success: never
    on_failure: always

script:
  - ansible-playbook --ask-become 
