---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing system packages
    apt: pkg="{{item}}" state=latest
    with_items:
      - vim-nox-py2
    when: ansible_distribution == 'Ubuntu'
