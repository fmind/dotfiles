---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing ubuntu packages
    apt: pkg="{{item}}" state=latest
    with_items:
      - lyx
      - pandoc
      - latexmk
      - texlive
      - texlive-science
      - texlive-math-extra
      - texlive-latex-extra
