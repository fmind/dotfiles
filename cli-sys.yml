---

- hosts: localhost
  tasks:

  - name: Installing ubuntu packages
    apt: pkg="{{item}}" state=latest
    become: yes
    with_items:
      - pv
      - jq
      - zsh
      - vim
      - git
      - tree
      - htop
      - curl
      - byobu
      - xclip
      - ipcalc
      - hexedit
      - parallel
      - imagemagick
      - fonts-hack-ttf
      - silversearcher-ag
