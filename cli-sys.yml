---

- hosts: localhost
  become: yes

  tasks:

  - name: Installing ubuntu packages
    apt: pkg="{{item}}" state=latest
    with_items:
      - jq
      - zsh
      - vim
      - git
      - tree
      - htop
      - curl
      - byobu
      - xclip
      - ipcalc
      - hexedit
      - parallel
      - imagemagick
      - fonts-hack-ttf
      - silversearcher-ag
