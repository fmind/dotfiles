---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing dependencies
    apt: pkg="{{item}}" state=latest
    with_items:
      - python-dev
      - python3-dev
      - python-setuptools
      - python3-setuptools
    when: ansible_distribution == 'Ubuntu'
