---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing system deps
    apt: pkg="{{item}}" state=latest
    with_items:
      - python3-dev
      - python3-setuptools
