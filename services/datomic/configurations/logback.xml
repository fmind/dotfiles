<configuration>

  <!-- prevent per-message overhead for jul logging calls, e.g. Hornet -->
  <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
    <resetJUL>true</resetJUL>
  </contextListener>

  <appender name="MAIN" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>${DATOMIC_LOG_DIR:-log}/%d{yyyy-MM-dd}.log</fileNamePattern>
      <maxHistory>72</maxHistory> 
    </rollingPolicy>
    <prudent>false</prudent> <!-- multi jvm safe, slower -->
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %-10contextName %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="datomic.cast2slf4j" level="WARN"/>

  <!-- uncomment to log storage access -->
  <!-- <logger name="datomic.kv-cluster" level="WARN"/> -->

  <!-- uncomment to log transactor heartbeat -->
  <!-- <logger name="datomic.lifecycle" level="WARN"/> -->

  <!-- uncomment to log transactions (transactor side) -->
  <!-- <logger name="datomic.transaction" level="WARN"/> -->

  <!-- uncomment to log transactions (peer side) -->
  <!-- <logger name="datomic.peer" level="WARN"/> -->

  <!-- uncomment to log the transactor log -->
  <!-- <logger name="datomic.log" level="WARN"/> -->

  <!-- uncomment to log peer connection to transactor -->
  <!-- <logger name="datomic.connector" level="WARN"/> -->

  <!-- uncomment to log storage gc -->
  <!-- <logger name="datomic.garbage" level="WARN"/> -->

  <!-- uncomment to log indexing jobs -->
  <!-- <logger name="datomic.index" level="WARN"/> -->

  <!-- these namespsaces create a ton of log noise -->
  <logger name="httpclient" level="WARN"/>
  <logger name="org.apache.commons.httpclient" level="WARN"/>
  <logger name="org.apache.http" level="WARN"/>
  <logger name="org.jets3t" level="WARN"/>
  <logger name="com.amazonaws" level="WARN"/>
  <logger name="com.amazonaws.request" level="WARN"/>
  <logger name="sun.rmi" level="WARN"/> 
  <logger name="net.spy.memcached" level="WARN"/>
  <logger name="com.couchbase.client" level="WARN"/>
  <logger name="org.apache.zookeeper" level="WARN"/>
  <logger name="com.ning.http.client.providers.netty" level="WARN"/>
  <logger name="org.eclipse.jetty" level="WARN"/>
  <logger name="org.hornetq.core.client.impl" level="WARN"/>
  <logger name="org.apache.tomcat.jdbc.pool" level="WARN"/>

  <logger name="datomic.cast2slf4j" level="WARN"/>

  <root level="warn">
    <appender-ref ref="MAIN"/>
  </root>
</configuration>
