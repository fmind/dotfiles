---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing hadoop service (fedora)
    dnf: pkg="hadoop-mapreduce" state=latest
    when: ansible_distribution == 'Fedora'
