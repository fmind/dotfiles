---

- hosts: localhost
  become: true

  tasks:

  - name: installing gnome
    dnf: pkg="{{item}}" state=latest
    with_items:
        - vlc
        - gimp
        - transmission
        - gnome-tweak-tool
        - gnome-shell-extension-pomodoro
