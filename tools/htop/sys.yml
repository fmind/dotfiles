---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing htop tool (ubuntu)
    apt: pkg="htop" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing htop tool (fedora)
    dnf: pkg="htop" state=latest
    when: ansible_distribution == 'Fedora'
