---

- hosts: localhost
  become: true

  tasks:

  - name: Installing htop (ubuntu)
    apt: pkg="htop" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing htop (fedora)
    dnf: pkg="htop" state=latest
    when: ansible_distribution == 'Fedora'
