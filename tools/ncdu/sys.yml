---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing ncdu tool (ubuntu)
    apt: pkg="ncdu" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing ncdu tool (fedora)
    dnf: pkg="ncdu" state=latest
    when: ansible_distribution == 'Fedora'
