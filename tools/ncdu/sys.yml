---

- hosts: localhost
  become: true

  tasks:

  - name: Installing ncdu (ubuntu)
    apt: pkg="ncdu" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing ncdu (fedora)
    dnf: pkg="ncdu" state=latest
    when: ansible_distribution == 'Fedora'
