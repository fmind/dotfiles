# DEFAULT
alias df="df -hT"
alias du="du -h"
alias ps="ps aux"
alias wget="wget -c"
alias free="free -mt"
alias mkdir="mkdir -pv"
alias parallel="parallel --will-cite"

# EDITION
alias ca="cat"
alias pz="pigz"
alias puz="unpigz -c"
alias v="$EDITOR"
alias vi="$EDITOR"
alias vim="$EDITOR"
alias vig="$EDITOR -g"
alias vip="$EDITOR -p"
alias vir="$EDITOR -R"
alias vis="sudo $EDITOR"
alias e="emacsclient -a='' -qc"

# NAVIGATION
alias l="ls --color=auto -CF"
alias ll="l -lFh"
alias la="l -lAFh"
alias lr="l -lRFh"
alias lt="l -ltFh"
alias lh="l -ld .*"
alias cb="cd $OLDPWD"
function c {
  cd "$@" && l
}

# INTEGRATION
alias x="exit"
alias r="clear"
alias q="ranger"
alias o="xdg-open"
alias u='nautilus . &'
alias dark="xbacklight -set 10"
alias bright="xbacklight -set 100"
alias xc="xsel --clipboard --output"
alias xck="xsel --clipboard --clear"

# INFORMATION
alias gg="google"
alias hh="history"
alias ascii="man ascii"
alias units="man units"
alias release="lsb_release -c"
alias cmount="mount | column -t"
alias extip='curl icanhazip.com'

# DISTRIBUTION
alias notify='notify-send'
alias n='notify'

# CONFIGURATION
alias virc='$EDITOR ~/.vimrc'
alias zsrc='source ~/.zshrc'
alias zrc='$EDITOR ~/.zshrc && zsrc'
alias bsrc='source ~/.bashrc'
alias brc='$EDITOR ~/.bashrc && bsrc'
alias asrc='source ~/.shell_aliases'
alias arc='$EDITOR ~/.shell_aliases && asrc'
alias esrc='source ~/.shell_environ'
alias erc='$EDITOR ~/.shell_environ && esrc'
alias psrc='source ~/.shell_private'
alias prc='$EDITOR ~/.shell_private && psrc'
alias warprc='$EDITOR ~/.warprc'
alias pypirc='$EDITOR ~/.pypirc'
alias sshrc='$EDITOR ~/.ssh/config'
alias idearc='$EDITOR ~/.ideavimrc'
alias emacsrc='$EDITOR ~/.spacemacs'
alias latexmkrc='$EDITOR ~/.latexmkrc'
alias cookierc='$EDITOR ~/.cookiecutterrc'

# ADMINISTRATION
alias cpr="cp -r"
alias rmr="rm -r"
alias rmrf="rm -rf"
alias chx="chmod u+x "
alias kila='killall -9'
alias tunnell='ssh -nNTL'
alias tunnelr='ssh -nNTR'
alias srv='sudo service'
alias rsinc="rsync -avzu"
alias pwgen="pwgen -N1 -y"
alias psmem='ps aux | sort -nk 4'
alias psproc='ps aux | sort -nk 3'
backup() { cp "$1"{,.bak}; }

# SHARING
alias httpserve="python3 -m http.server"

# EDITING
alias texmake="latexmk -pv"
alias texview="latexmk -pv"
alias texlive="latexmk -pvc"
alias texclean="latexmk -c"
alias texpurge="latexmk -C"
alias imgsize="mogrify -resize "
alias imgwhite="mogrify -flatten "

# # SEARCHING
alias ka='fasd -al'
alias kf='fasd -fl'
alias kd='fasd -dl' 
alias kv='fasd -fe vim'
alias ko='fasd -fe xdg-open'
alias kai='fasd ali'
alias kfi='fasd -fli'
alias kdi='fasd -dli'
alias kvi='fasd -lie vim'
alias koi='fasd -lie xdg-open'
alias j='fasd_cd -d'
alias ji='fasd_cd -di'

# FILTERING
alias jqr="jq -r"
alias h="head"
alias hn="head -n"
alias t="tail"
alias tn="tail -n"
alias igrep="grep -i"
alias rgrep="grep -iR"
alias colt="column -t"
alias fid="find . -iname "
alias fih="find ~ -iname "
alias awktab="awk -F $'\t'"
function union {
    sort $1 $2 | uniq
}
function intersect {
    sort $1 $2 | uniq -d
}
function difference {
    sort $1 $2 $1 | uniq -u
}

# PROGRAMMING
## Git
alias g="git"
alias gm="git submodule"
alias gri="$EDITOR .gitignore"
alias grc="$EDITOR ~/.gitconfig"
function gh {
  git clone git@github.com:fmind/$1.git
}
function gho {
    git remote add origin "git@github.com:fmind/$(basename $PWD).git"
    git push -u origin master
}
## UML
alias uml="plantuml"
## OAR
alias oai="oarsub -I"
alias oas="oarsub -S"
## SRUN
alias sri="srun -p interactive --qos qos-interactive --pty bash -i"
## Project
alias m="make"
## Template
alias ck="cookiecutter"
## Ansible
alias an="ansible-playbook"
alias ans="ansible-playbook --ask-become"
alias ana="ansible-playbook --ask-become *.yml"
## Docker
alias dk="docker"
alias dkstart="service docker start"
alias dkrm="docker ps -a | grep 'weeks ago' | awk '{print $1}' | xargs --no-run-if-empty docker rm"
alias dkdata='docker run --rm --name data -v "$(pwd)":/home/jovyan/work -p 8888:8888 fmind/bigdata'
alias dkmongo='docker run --rm --name mongo --net=host -v "$(pwd)/data/db":/data/db mongo --smallfiles'
## Hadoop
alias haf="hdfs dfs"
## Watchdog
alias wdl="watchmedo log"
alias wdr="watchmedo auto-restart"
alias wds="watchmedo shell-command"

# LANGUAGES
## Java
alias ja="java"
alias jac="javac"
alias jaj="java -jar"
# ## Groovy
# alias gro="groovy"
# alias gron="groovy -n"
# alias grop="groovy -p"
# alias gros="groovysh"
# alias grod="groovydoc"
## Racket
alias rc="raco"
alias rk="racket"
alias rkclj="racket -I rackjure"
alias rkrc="$EDITOR ~/.racketrc"
## Clojure
alias clrc="$EDITOR ~/.lein/profiles.clj"
alias clrb="$EDITOR ~/.boot/profile.boot"
alias clrp="$EDITOR ~/.boot/boot.properties"
## Haskell
alias hh="stack"
alias ho="hayoo"
alias hg="hoogle"
alias hn="stack new"
alias he="stack eval"
alias hx="stack exec"
alias hr="stack repl"
alias ht="stack test"
alias hb="stack build"
alias hm="stack bench"
alias hs="stack setup"
alias hc="stack config"
alias hk="stack docker"
alias hu="stack update"
alias hv="stack upgrade"
alias hd="stack haddock"
alias hi="stack install"
alias hww="stack exec site watch"
alias hwb="stack exec site build"
alias hl="stack list-dependencies"
alias hp="stack repl --test --bench"
alias hrc="$EDITOR ~/.stack/config.yaml"
# shell auto-completion with stack
autoload -U +X compinit && compinit
autoload -U +X bashcompinit && bashcompinit
eval "$(stack --bash-completion-script stack)"
## Go
# alias mygo="cd $GOREPO"
## Nim
# alias nb="nimble"
# alias nbt="nimble init"
# alias nbt="nimble tasks"
# alias nbs="nimble search"
# alias nbp="nimble publish"
# alias nbi="nimble install"
# alias nbu="nimble uninstall"
# alias nd="nim doc2"
# alias nk="nim check"
# alias ni="rlwrap nim secret"
# alias nc="nim compile -w:on"
# alias nr="nim compile --run -w:on"
# alias no="nim --compileOnly -w:on"
## Pip
alias pp="pip"
alias ppl="pip list"
alias ppul="pip list --user"
alias pps="pip search"
alias ppus="pip search --user"
alias ppi="pip install"
alias ppui="pip install --user"
alias ppu="pip uninstall"
alias ppuu="pip uninstall --user"
alias ppf="pip freeze > requirements.txt"
alias ppuf="pip freeze --user > requirements.txt"
alias ppr="pip install -r requirements.txt"
alias ppur="pip install --user -r requirements.txt"
## Conda
alias pac="conda create -y"
alias paa="source activate"
alias pad="source deactivate"
alias pav="conda env export > environment.yml"
## Python
alias p="python"
alias py="python"
alias pyh="pydoc"
alias pyt="pytest"
alias py2="python2"
alias py3="python3"
alias pyr="ptpython"
alias pys="python setup.py"
## Jupyter
alias pn="jupyter-notebook --NotebookApp.token="
alias pnp="ipcluster start -n $(nproc)"
## VirtualEnv
alias pe="source venv/bin/activate"
alias ped="deactivate"
function pei {
  virtualenv "venv" "$@" && echo 'source "$(dirname $0)/venv/bin/activate"' > .env
}
