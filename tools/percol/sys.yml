---

- hosts: localhost
  become: true

  tasks:

  - name: Installing percol (ubuntu)
    apt: pkg="percol" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing percol (fedora)
    dnf: pkg="percol" state=latest
    when: ansible_distribution == 'Fedora'
