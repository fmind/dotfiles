---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  # not (yet) packaged on ubuntu ...
  # - name: Installing htop tool (ubuntu)
  #   apt: pkg="htop" state=latest
  #   when: ansible_distribution == 'Ubuntu'

  - name: Installing conda tool (fedora)
    dnf: pkg="conda-activate" state=latest
    when: ansible_distribution == 'Fedora'
