---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing shellcheck tool (ubuntu)
    apt: pkg="shellcheck" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing shellcheck tool (fedora)
    dnf: pkg="ShellCheck" state=latest
    when: ansible_distribution == 'Fedora'
