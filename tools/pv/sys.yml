---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing pv tool (ubuntu)
    apt: pkg="pv" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing pv tool (fedora)
    dnf: pkg="pv" state=latest
    when: ansible_distribution == 'Fedora'
