---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - name: Deploying latexmk configuration file
    file: src="{{playbook_dir}}/latexmkrc" dest="~/.latexmkrc" state=link force=yes
