---

- hosts: localhost
  become: true

  tasks:

  - name: Installing rlwrap (ubuntu)
    apt: pkg="rlwrap" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing rlwrap (fedora)
    dnf: pkg="rlwrap" state=latest
    when: ansible_distribution == 'Fedora'
