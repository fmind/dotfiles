---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing fasd tool (ubuntu)
    dnf: pkg="fasd" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing fasd tool (fedora)
    dnf: pkg="fasd" state=latest
    when: ansible_distribution == 'Fedora'
