---

- hosts: localhost
  become: true

  tasks:

  - name: Installing ag (ubuntu)
    apt: pkg="silversearcher-ag" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing ag (fedora)
    dnf: pkg="the_silver_searcher" state=latest
    when: ansible_distribution == 'Fedora'
