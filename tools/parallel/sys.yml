---

- hosts: localhost
  become: true

  tasks:

  - name: Installing ag (ubuntu)
    apt: pkg="parallel" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing ag (fedora)
    dnf: pkg="parallel" state=latest
    when: ansible_distribution == 'Fedora'
