---

- hosts: localhost
  become: true

  tasks:

  - name: Installing curl (ubuntu)
    apt: pkg="curl" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing curl (fedora)
    dnf: pkg="curl" state=latest
    when: ansible_distribution == 'Fedora'
