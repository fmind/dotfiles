---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Adding umake repository
    apt_repository: repo="ppa:ubuntu-desktop/ubuntu-make" update_cache=yes

  - name: Installing umake tool
    apt: pkg="ubuntu-make" state=latest
