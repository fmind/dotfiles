---

- hosts: localhost
  connection: local

  tasks:

  - shell: test -d ~/.sdkman
    ignore_errors: True
    register: sdkcheck

  - name: Installing sdkman
    shell: curl -s "https://get.sdkman.io" | bash
    when: sdkcheck|failed
