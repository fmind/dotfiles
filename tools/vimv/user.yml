---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - name: Creating ~/bin directory
    file: path="~/bin" state=directory

  - name: Download foo.conf
    get_url: url="https://raw.githubusercontent.com/thameera/vimv/master/vimv" dest="~/bin/vimv" mode="0750"
