---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Adding ansible repository
    apt_repository: repo="ppa:ansible/ansible" update_cache=yes

  - name: Installing ansible tool
    apt: pkg="ansible" state=latest
