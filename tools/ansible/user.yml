---

- hosts: localhost
  connection: local
  become: false

  tasks:

  - name: Deploying ansible configuration file
    file: src="{{playbook_dir}}/ansible.cfg" dest="~/.ansible.cfg" state=link force=yes
