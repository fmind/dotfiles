---

- hosts: localhost
  become: true

  tasks:

  - name: Installing ranger (ubuntu)
    apt: pkg="ranger" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing ranger (fedora)
    dnf: pkg="ranger" state=latest
    when: ansible_distribution == 'Fedora'
