---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing zsh tool (ubuntu)
    apt: pkg="zsh" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing zsh tool (fedora)
    dnf: pkg="zsh" state=latest
    when: ansible_distribution == 'Fedora'
