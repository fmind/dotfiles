---

- hosts: localhost
  become: false

  tasks:

  - name: deploying git ignore
    file: src="{{playbook_dir}}/gitignore" dest="~/.gitignore" state=link force=yes

  - name: deploying git configuration
    file: src="{{playbook_dir}}/gitconfig" dest="~/.gitconfig" state=link force=yes
