---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing git tool (ubuntu)
    apt: pkg="git" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing git tool (fedora)
    dnf: pkg="git" state=latest
    when: ansible_distribution == 'Fedora'
