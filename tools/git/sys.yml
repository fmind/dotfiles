---

- hosts: localhost
  become: true

  tasks:

  - name: Installing git (ubuntu)
    apt: pkg="git" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing git (fedora)
    dnf: pkg="git" state=latest
    when: ansible_distribution == 'Fedora'
