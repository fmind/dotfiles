---

- hosts: localhost
  become: true

  tasks:

  - name: Installing pigz (ubuntu)
    apt: pkg="pigz" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing pigz (fedora)
    dnf: pkg="pigz" state=latest
    when: ansible_distribution == 'Fedora'
