---

- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Installing pigz tool (ubuntu)
    apt: pkg="pigz" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing pigz tool (fedora)
    dnf: pkg="pigz" state=latest
    when: ansible_distribution == 'Fedora'
