---

- hosts: localhost
  become: true

  tasks:

  - name: Installing imagemagick (ubuntu)
    apt: pkg="imagemagick" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing imagemagick (fedora)
    dnf: pkg="ImageMagick" state=latest
    when: ansible_distribution == 'Fedora'
