---

- hosts: localhost
  tasks:

  - name: Installing ubuntu packages
    apt: pkg="{{item}}" state=latest
    become: yes
    with_items:
      - lyx
      - pandoc
      - latexmk
      - texlive
      - texlive-science
      - texlive-math-extra
      - texlive-latex-extra
