---

- hosts: localhost
  become: true

  tasks:

  - name: installing tex
    package: pkg="{{item}}"
    with_items:
      - texlive
      - texlive-xetex
      - texlive-collection-science
      - texlive-collection-mathextra
      - texlive-collection-latexextra
