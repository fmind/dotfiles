---

- hosts: localhost
  become: false

  tasks:

  - name: Installing jupyter lab
    pip: name="jupyterlab" executable="pip3" extra_args="--user" state=latest

  - name: Installing jupyter notebook
    pip: name="notebook" executable="pip3" extra_args="--user" state=latest
