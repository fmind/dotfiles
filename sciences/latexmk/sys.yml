---

- hosts: localhost
  become: true

  tasks:

  - name: Installing latexmk (ubuntu)
    apt: pkg="latexmk" state=latest
    when: ansible_distribution == 'Ubuntu'

  - name: Installing latexmk (fedora)
    dnf: pkg="latexmk" state=latest
    when: ansible_distribution == 'Fedora'
