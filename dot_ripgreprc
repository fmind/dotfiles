# Search hidden files (but keep .git ignored)
--hidden
--glob=!.git/

# Follow symlinks
--follow

# Case-insensitive search (unless uppercase)
--smart-case

# Truncate very long lines
--max-columns=150
--max-columns-preview

# Modern features
--hyperlink
